import { createTheme } from "@mui/material";

const custonmTheme = createTheme({
    palette: {
        mode: "light",
        primary: {
            main: '#00927c',
        },
        secondary: {
            main: '#f50057',
        },
    },
})

export default custonmTheme;